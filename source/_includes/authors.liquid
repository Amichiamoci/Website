{% unless site.authors == empty and site.authors.size > 0 %}
  <strong>Staff attuale</strong>
  <ul>
    {% for author in site.authors %}
      <li>
        {% include author.liquid %}
      </li>
    {% endfor %}
  </ul>
{% endunless %}

{% unless site.ex_authors == empty and site.ex_authors.size > 0 %}
  <strong>Ex staffisti</strong>
  <ul>
    {% for author in site.ex_authors %}
      <li>
        {% include author.liquid %}
      </li>
    {% endfor %}
  </ul>
{% endunless %}